import styled from '@emotion/styled'

import { PageTitle } from '../page'
import { colors } from '../../styles/variables'

export const ProjectTitle = styled(PageTitle)`
  margin-top: 0;
  color: ${colors.green30};
`
